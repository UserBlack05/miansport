@extends('layouts.app') @section('title','MianSport — '.$title) @section('content')<section><p class="kicker">MIANSPORT</p><h1>{{ $title }}</h1>@isset($description)<p>{{ $description }}</p>@endisset<div class="grid">@forelse($articles as $article)<article class="card"><p class="kicker">{{ $article->type }} · {{ $article->category->name }}</p><h3><a href="{{ route('articles.show',$article) }}">{{ $article->title }}</a></h3><p>{{ $article->excerpt }}</p></article>@empty<p>Aucun contenu disponible.</p>@endforelse</div>{{ $articles->links() }}</section>@endsection
